# Struere API key for the dev environment.
# Create with: cd struere && struere keys create --name "vite-spa-dev" --env development --json
# Copy the `key` field into .env.local (gitignored).
VITE_STRUERE_API_KEY=
